변수의 범위
  변수 선언시 속해있는 중괄호 안에서만 사용 가능하다!

psuedo code-코드의 논리를 문자로 나열한 것

switch () {}
  웹어플리케이션 개발에는 거의 쓰지 않는다! ==밖에 할 줄 모르기 때문
  if는 여러 비교가 가능하기 떄문에 if를 사용한다
  엔진 개발시에는 종종 쓰인다

반복 제어문 While
  while (조건(비교, 논리 연산자)) {
  반복될 코드 블럭
  }
  조건이 언제 달성될 지 알 수 없다
  웹어플리케이션 개발에는 쓰지 않음! 그 어플은 반드시 죽는다

범위 반복자 for 굉장히 중요!
  지정된 범위까지만 반복!
  for(초기값; 반복조건; 증감식) {
    반복할 코드
  }
  for에 설정된 변수는 for문 안에서만 쓰인다
  반복에 필요한 i는 건드려선 안된다
	보통 i++을 쓰는게 일반적이다
  중첩해서 사용 가능
  
@@@코드@@@

Scanner keyboard = new Scanner(System.in);  
  이 코드는 콘솔에 입력한 값을 
  변수로 사용할 수 있도록 해줍니다.
Ctrl + Shift + O : 임포트(기능가져오기)
keyboard.nextInt();
-nextInt(); int(자료형)로 받겠다



@@@오늘 쓴 단축키@@@

Ctrl + Shift + O : 임포트(기능가져오기)  예)Scanner, Random...
범위 설정 후 컨트롤 / : 주석처리
