과제 컨펌
  클래스 이름 시작은 대문자, 언더바나 숫자도 사용하면 안된다
  조건식에는 숫자를 직접 넣는것은 바람직하지 않다 
  -보안상의 이유나 수정에 어려움이 있음 * 상수 처리해야 수정이 용이! (final 대문자변수)
  조건식 안에서도 변수를 정의할 수 있음
  문제 2번에서 -1입력시 종료된다는 걸 알리지 않았다
  들여쓰기를 잘해서 가독성을 확보해주기
  이해하기 쉬운 변수명 중요
  중괄호 들여쓰기가 중요하다
  사용자 입장을 생각한 가이드를 적어주면 좋다
  코드의 일관성이 코드의 품질이다

메소드
  하나의 기능을 하는 코드 묶음
  정의 규칙은 통째로 암기
  -접근제한자 예약어 반환타입 메소드명-
  main 위에 생성
  논리적인 연결점이 없을 때 메소드로 나눠준다
  변수를 여러개 넣을 수 있다. 콤마로 구분 예)(변수, 변수)
  return - 값을 돌려준다, 메소드를 종료시킨다 (리턴 이후의 코드는 동작하지 않는다) 2개의 역할. 
  반환하는 메소드의 이름에 앞에 get을 붙여주는게 일반적
  목적에 따라 반환을 하고 하지 않고 결정한다
  메소드는 하나의 값만 반환할 수 있다

디버깅
  -브레이킹 포인트 라인 더블클릭
  -F11
  -F6으로 한번씩 진행
  -F5는 작동하는 메소드 확인
  -F7로 다시 복귀
  -F8로 다음 브레이킹 포인트까지 진행
